# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database (optional - comment out if not using database)
# sqlalchemy==2.0.23
# alembic==1.12.1
# psycopg2-binary==2.9.9

# File processing - UPGRADED
PyMuPDF==1.23.8  # Better PDF parsing (also known as fitz)
python-docx==1.1.0
python-magic==0.4.27
pdfplumber==0.10.3  # Alternative PDF parser
Pillow==10.1.0  # Image processing for format checks

# Natural Language Processing - OPTIMIZED FOR DEPLOYMENT
# Install CPU-only torch first to save space (500MB vs 2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
sentence-transformers==2.2.2  # Semantic embeddings matching
scikit-learn==1.3.2
keybert==0.8.3  # Keyword extraction
transformers==4.35.2  # Hugging Face transformers

# AI/LLM Integration (Optional)
google-generativeai==0.3.2  # Google Gemini (FREE tier available)

# Data processing
pandas==2.1.3
numpy==1.25.2

# Environment and configuration
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication and security (optional - comment out if not using auth)
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4

# CORS and middleware
fastapi-cors==0.0.6

# Testing (optional - comment out for faster deployment)
# pytest==7.4.3
# pytest-asyncio==0.21.1
httpx==0.25.2

# Development tools (uncomment for local development)
black==23.11.0
isort==5.12.0
ruff==0.1.6  # Fast modern linter (replaces flake8)
mypy==1.7.1  # Type checker
pre-commit==3.6.0  # Git hooks for automated checks
