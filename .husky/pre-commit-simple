#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running simple pre-commit checks..."

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

print_status() {
    echo -e "${BLUE}📝 $1${NC}"
}

print_success() {
    echo -e "${GREEN}✅ $1${NC}"
}

print_error() {
    echo -e "${RED}❌ $1${NC}"
}

# 1. Type checking (skip for now due to test issues)
print_status "Skipping type checking due to test configuration issues..."

# 2. Linting
print_status "Linting code..."
npm run lint
if [ $? -ne 0 ]; then
    print_error "Linting failed. Please fix linting errors before committing."
    exit 1
fi
print_success "Linting passed"

# 3. Code formatting
print_status "Checking code formatting..."
npm run format:check
if [ $? -ne 0 ]; then
    print_error "Code formatting check failed. Please run 'npm run format' to fix formatting issues."
    exit 1
fi
print_success "Code formatting check passed"

# 4. Build check (to ensure code compiles)
print_status "Checking build..."
npm run build
if [ $? -ne 0 ]; then
    print_error "Build failed. Please fix build errors before committing."
    exit 1
fi
print_success "Build check passed"

print_success "Simple pre-commit checks passed! Proceeding with commit..."
